[project]
name = "job-scheduler-env"
version = "0.1.0"
description = "OpenEnv environment"
dependencies = [
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:app"
